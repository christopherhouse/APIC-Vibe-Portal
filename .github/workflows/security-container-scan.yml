name: "Security: Container Scanning"

on:
  push:
    branches:
      - main
    paths:
      - 'src/frontend/**'
      - 'src/shared/**'
      - 'src/bff/**'
      - 'src/indexer/**'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/security-container-scan.yml'
      - '.trivyignore'
  pull_request:
    branches:
      - main
    paths:
      - 'src/frontend/**'
      - 'src/shared/**'
      - 'src/bff/**'
      - 'src/indexer/**'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/security-container-scan.yml'
      - '.trivyignore'

permissions:
  contents: read
  security-events: write

jobs:
  trivy-scan-frontend:
    name: Trivy Scan — Frontend
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Build Frontend Docker image
        run: docker build -t frontend:scan -f src/frontend/Dockerfile .

      - name: "Trivy: show CRITICAL/HIGH vulnerabilities (table)"
        uses: aquasecurity/trivy-action@0.35.0
        with:
          image-ref: frontend:scan
          format: table
          severity: CRITICAL,HIGH
          exit-code: '1'
          trivyignores: .trivyignore

      - name: "Trivy: generate SARIF report"
        uses: aquasecurity/trivy-action@0.35.0
        if: always()
        with:
          image-ref: frontend:scan
          format: sarif
          output: trivy-frontend-results.sarif
          severity: CRITICAL,HIGH
          exit-code: '0'
          trivyignores: .trivyignore

      - name: Upload Trivy scan results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@v4
        if: always() && hashFiles('trivy-frontend-results.sarif') != ''
        with:
          sarif_file: trivy-frontend-results.sarif
          category: trivy-frontend

  trivy-scan-bff:
    name: Trivy Scan — BFF
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Build BFF Docker image
        run: docker build -t bff:scan -f src/bff/Dockerfile .

      - name: "Trivy: show CRITICAL/HIGH vulnerabilities (table)"
        uses: aquasecurity/trivy-action@0.35.0
        with:
          image-ref: bff:scan
          format: table
          severity: CRITICAL,HIGH
          exit-code: '1'
          trivyignores: .trivyignore

      - name: "Trivy: generate SARIF report"
        uses: aquasecurity/trivy-action@0.35.0
        if: always()
        with:
          image-ref: bff:scan
          format: sarif
          output: trivy-bff-results.sarif
          severity: CRITICAL,HIGH
          exit-code: '0'
          trivyignores: .trivyignore

      - name: Upload Trivy scan results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@v4
        if: always() && hashFiles('trivy-bff-results.sarif') != ''
        with:
          sarif_file: trivy-bff-results.sarif
          category: trivy-bff

  trivy-scan-indexer:
    name: Trivy Scan — Indexer
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Build Indexer Docker image
        run: docker build -t indexer:scan -f src/indexer/Dockerfile .

      - name: "Trivy: show CRITICAL/HIGH vulnerabilities (table)"
        uses: aquasecurity/trivy-action@0.35.0
        with:
          image-ref: indexer:scan
          format: table
          severity: CRITICAL,HIGH
          exit-code: '1'
          trivyignores: .trivyignore

      - name: "Trivy: generate SARIF report"
        uses: aquasecurity/trivy-action@0.35.0
        if: always()
        with:
          image-ref: indexer:scan
          format: sarif
          output: trivy-indexer-results.sarif
          severity: CRITICAL,HIGH
          exit-code: '0'
          trivyignores: .trivyignore

      - name: Upload Trivy scan results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@v4
        if: always() && hashFiles('trivy-indexer-results.sarif') != ''
        with:
          sarif_file: trivy-indexer-results.sarif
          category: trivy-indexer

  trivy-config-scan:
    name: Trivy Config Scan — Dockerfiles
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Run Trivy config scanner on Dockerfiles
        uses: aquasecurity/trivy-action@0.35.0
        with:
          scan-type: config
          scan-ref: .
          format: table
          severity: CRITICAL,HIGH,MEDIUM
          exit-code: '1'
          trivy-config: |
            scan:
              skip-dirs:
                - node_modules
                - .git
                - infra
